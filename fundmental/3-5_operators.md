+, -, *, /, %

n++, ++n

short circuit:
x != 0 && 1/x > 5; // does this statement raise error?

ternary ?: operator:
x > 0 ? 0 : 1

&, |, ^, ~, >>, <<, >>>
12 <<< 2; // valid statement?

a && b || c

a += b += c
